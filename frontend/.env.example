# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT_ID=your-project-id
BIGQUERY_DATASET=skyspace_dataset

# Option 1: Service Account Key File Path
GOOGLE_CLOUD_KEY_FILE=./path/to/service-account-key.json

# Option 2: Service Account Credentials (JSON string)
# GOOGLE_CLOUD_CREDENTIALS={"type":"service_account","project_id":"..."}

# For production, consider using Google Cloud IAM roles instead of service account keys